FROM ubuntu:latest

RUN apt-get update && apt-get install wget -y
RUN wget https://github.com/badaix/snapcast/releases/download/v0.4.0/snapclient_0.4.0_amd64.deb

RUN dpkg -i --force-all snapclient_0.4.0_amd64.deb
RUN apt-get -f install -y

